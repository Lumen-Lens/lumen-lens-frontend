"use client";

// Placeholder component — wire up to a backend history endpoint or localStorage.
export function RecentGenerations() {
  return (
    <section className="card space-y-4">
      <h2 className="font-semibold text-white text-lg">Recent Generations</h2>
      <p className="text-gray-500 text-sm">
        Your generation history will appear here once you start generating SDKs.
      </p>
    </section>
  );
}
